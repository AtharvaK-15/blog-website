<%- include("Partials/header"); -%> 

<!-- ejs tags should be on every line -->

<h1>Home</h1>

<p><%= StartingFile %> </p>

<% posts.forEach(function(post){ %> 
    <h1> <%= post.title %>  </h1>
    <p>
     <%= post.content.substring(0,200) + "..." %>  
     <a href="/posts/<%= post.title %> ">Read More</a>    
    </p>
<% }) %> 


<%- include("Partials/footer"); -%>     